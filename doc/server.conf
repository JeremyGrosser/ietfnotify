[fields-area]
required = tag
optional = name, director, advisor

[fields-wg]
required = tag
optional = name, chair, charter, area, list-email, list-archive, list-subscribe

[general]
socktype = inet
bindaddr = 127.0.0.1
bindport = 5224
recvbuffer = 1024

[archive]
uuid_dir = /home/synack/ietfnotify/data/uuid
date_dir = /home/synack/ietfnotify/data/date

[notifier]
mysqlhost = localhost
mysqluser = synack
mysqlpass = rtz2096
mysqldb = ietfnotify

[notify-atom]
feedlength = 10

[notify-email]
smtphost = 127.0.0.1
smtpport = 25
smtpfrom = notifications@tools.ietf.org

[notify-jabber]
jid = jtest@jabber.org
password = rtz2096

[fields-any]
required = tag

[fields-event]
required = tag
optional = url, source, author, title

[fields-draft]
required = tag
optional = author, title, url, pages

[fields-announcement]
required = tag
optional = text

